<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31"
        >

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Move this activity inside the <application> block -->
        <activity android:name=".MakePickupRequestActivity"></activity>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".RegisterActivity"></activity>
        <activity android:name=".HomeActivity" ></activity>
        <activity android:name=".PickupRequestActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".EditAccountActivity"></activity>
        <activity android:name=".ManageDeliveryPreferencesActivity"></activity>
        <activity android:name=".SelectRunnerActivity"></activity>
        <activity android:name=".TrackPickupActivity"></activity>
        <activity android:name=".ReceiveStatusNotificationsActivity"></activity>
        <activity android:name=".ReceiveRunnerUpdatesActivity"></activity>
        <activity android:name=".ReceiveCompletionConfirmationActivity"></activity>
        <activity android:name=".PaymentActivity"></activity>
        <activity android:name=".RateRunnerActivity"></activity>
        <activity android:name=".FeedbackActivity"></activity>
    </application>
</manifest>
